# Migration `20190914220552-inicio`

This migration has been generated by Ivan Pianetti at 9/14/2019, 10:05:52 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190914220552-inicio
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,58 @@
+// https://github.com/prisma/prisma2/tree/master/docs
+datasource db {
+  provider = "mysql"
+  url      = env("MYSQL_URL")
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model Country {
+  id            Int         @id
+  iso_code      String      @unique
+  name          String
+  active        Boolean
+  provincies    Province[]
+
+  @@map("country")
+}
+
+model Province {
+  id            Int             @id
+  iso_code      String          @unique
+  code          String          @unique
+  country       Country         @map("country_id")
+  name          String
+  active        Boolean
+  cities        City[]
+  departments   Department[]
+
+  @@map("province")
+}
+
+model City {
+  id            Int             @id
+  code          String          @unique
+  name          String
+  province      Province        @map("province_id")
+  lat           Float?
+  lon           Float?
+  active        Boolean
+
+  @@map("city")
+}
+
+model Department {
+  id            Int             @id
+  code          String          @unique
+  name          String
+  province      Province        @map("province_id")
+  active        Boolean
+
+  @@map("department")
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190914220552-inicio)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190914220552-inicio'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
